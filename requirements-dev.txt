# ============================================================
# Development-only dependencies
# ============================================================
-r requirements.txt

# CI lint stack (explicit so .github/workflows/python-app.yml can rely on
# these binaries being installed even if the workflow conditional changes).
flake8>=7.0.0
pytest>=8.0.0

# Modern linter / formatter (replaces flake8 long-term; config in
# pyproject.toml). Kept alongside flake8 to keep CI green during the
# migration window.
ruff>=0.6.0

# Static type checking
mypy>=1.10.0

# Hooks / automation
pre-commit>=3.8.0

# ------------------------------------------------------------
# Notes
# ------------------------------------------------------------
# * `skills/fireworks-tech-graph` does not require any extra Python
#   dependency here; it uses the system tool `rsvg-convert`.
